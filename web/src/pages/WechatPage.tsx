import { Construction } from "lucide-react";
import { EmptyState } from "@/components/ui/empty-state";

export default function WechatPage() {
  return (
    <EmptyState
      icon={Construction}
      title="微信回复 — Phase 6"
      description="联系人 / 对话 / 客户档案 / 建议回复；接 9203 wxauto sidecar (Win) + macOS mock"
    />
  );
}
